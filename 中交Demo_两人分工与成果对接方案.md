# 中交 Demo：两人分工与成果对接方案

> 技术栈已由用户补充确认：Java＋Spring、Next.js＋React、MongoDB、Apifox、Git、Docker。Git统一代码版本与环境配置，Docker重建运行环境。最新架构、职责和接口以[中交Demo_技术栈与接口分工.md](中交Demo_技术栈与接口分工.md)为准；下文的本地技能方案、旧人员分配和五天估算保留为此前设计记录，不作为当前执行约定。

本方案补充《中交Demo方案.md》，专门回答“没有购买GPT团队版，两位同事分别使用个人ChatGPT账号辅助开发，如何把三个业务汇集到WorkBuddy，开发成果和业务文件如何交接”。用户已明确：没有团队版指GPT团队版，不代表WorkBuddy缺少某项能力，也不代表没有模型API。当前交付为设计方案，业务模块尚未在本任务中开发或验证。

## 1. 按原始需求确定交付边界

依据工作区三份《场景说明.txt》，以及另一任务对素材核对后形成的《中交Demo方案.md》：

| 业务 | 原始要求及已整理范围 | 必须交付的业务成果 |
| --- | --- | --- |
| 立项材料审查 | 对照技术审查意见单审核五份材料，报告与原表样式一致；现有方案记录原表有43个非空审核要求行 | 原格式审查Excel、逐项状态、规则及原文证据 |
| 立项书起草 | 需求分析表生成项目立项说明书；现有方案按序号整理为7组需求，模板有九个主章节 | 可编辑Word初稿、需求与章节对应关系、待补充事项 |
| Excel拆分 | 按单位生成单个单位明细表；现有方案记录18,884条明细、20列、50家单位 | 50份单位Excel、压缩包、全量核对报告 |

这些数量采用另一任务对素材的核对结果；本次重新读取了原始场景说明和更新后的方案，没有重新执行工作簿全量比对。

三个业务均属于本次核心交付。在线复核协同在原始题目中列为扩展。需求表里的投标、考勤、巡检等内容，是写立项书的输入，不代表本次要开发这些系统。

三组素材分别用于三个独立测试。统一接入表示它们使用同一个入口和成果管理约定，不表示三组素材属于同一个项目或必须依次流转。生成的新立项书如需再审查，要另建任务，明确材料范围和适用规则。

## 2. 推荐的最小结构

```text
两位同事的个人AI开发工具
          ↓ 辅助开发
同一份项目代码、接口约定、模板和运行说明
          ↓ 合并并部署到演示电脑
WorkBuddy：审材料 / 写立项书 / 拆Excel
          ↓ 按任务说明调用
三个业务模块 + 公共文件处理与校验工具
          ↓
每次任务独立保存：输入副本、处理中间数据、最终文件、核对记录
          ↓
WorkBuddy返回结果位置或下载入口 → Office打开复核
```

采用本地技能包配合处理脚本的方案，先验证演示电脑上的工具执行与文件访问能力。此阶段无需为三个业务分别建设网页、数据库和独立后端。

WorkBuddy官方说明支持通过技能封装脚本和流程，也提供连接器接入路径。当前安装版本、账号权益、模型额度、执行权限和可下载性仍需实测；官方能力说明不能代替本机验证。

对接方式的取舍：

| 方式 | 适用情况 | 本次安排 |
| --- | --- | --- |
| 本地技能包＋处理脚本 | 一台演示电脑，主要处理上传或本地文件 | 优先采用 |
| MCP封装同一套工具 | 需要稳定工具参数，或以后调用远程业务服务 | 本地方案跑通后按需要增加 |
| 独立网页＋后端＋模型API | 需要多人浏览器使用、集中任务管理 | 后续正式化时再评估 |

本地试用包与公开连接器市场发布是不同交付。Demo不需要先发布市场；如后续采用市场连接器，按届时官方规范完成封装和验证。

## 3. 没有GPT团队版，两个人怎样保持一致

本方案通过共享项目文件实现协作，不把购买GPT团队版作为合并代码或交付Demo的前提。两位同事各用自己的ChatGPT账号辅助分析、写代码和调试，将确认后的成果保存在同一个项目中。

两人分别给自己的ChatGPT提供相同版本的公共材料，确保开发依据一致：

| 公共材料 | 应写清楚什么 |
| --- | --- |
| 项目总说明 | 三个考题、输入素材、输出要求、范围与验收标准 |
| 接口约定 | 输入输出字段、状态、目录和文件命名，修改需双方同步 |
| 开发约定 | 依赖版本、模块边界、模板位置、配置方式 |
| 进度与交接记录 | 谁负责什么、已完成内容、下一步、已知问题、当前版本 |

把公共材料作为统一上下文；不假定一个账号中的会话会自动知道另一个账号的决定。个人ChatGPT会话用于辅助开发，项目文件作为双方共同确认的依据。

每次交接包含：实际代码和模板、调用说明、样例输入输出、验证记录、版本号。接收方在自己的ChatGPT或开发工具中提供这些文件，再继续修改或集成。仅转发聊天截图或一段结论不足以完成交接。

建议两人给各自ChatGPT使用下面的开工说明：

> 请先读取项目总说明、接口约定、开发约定及最新交接记录。本次只实现分配给我的业务模块，遵循现有输入输出格式。需要调整公共接口时先列出影响，由项目负责人统一更新。交付时给出完整代码、运行说明、样例及验证结果，并列出尚未完成或未验证的事项。

ChatGPT的项目功能可以组织聊天、文件和指令；具体入口与文件访问方式见[官方项目说明](https://learn.chatgpt.com/zh-Hans/docs/projects)。本方案采用文件交接，不依赖跨账号共享项目或自动同步会话的产品功能。

开发辅助工具与最终运行环境分别安排：

| 用途 | 推荐安排 |
| --- | --- |
| 开发时分析需求、写代码、修改提示词 | 两人各自的ChatGPT账号，能力和额度按实际账号使用 |
| 合并与验收 | 共享代码仓库或版本化交接包，统一演示电脑 |
| 演示时审查、起草、拆表 | WorkBuddy调用合并后的三个业务模块 |

演示时模型由WorkBuddy内部编排，还是由模块调用独立API，应根据实际运行环境选择；用户尚未说明该条件。本方案不从“没有GPT团队版”推断API不可用，也不要求为此更换运行架构。

若采用WorkBuddy内部模型，处理流程为“脚本解析→模型判断或起草→结构化结果落盘→脚本校验与出文件”；若采用独立API，则模块通过统一模型配置执行。两条路径共用业务数据契约，均需要实际验证。账号密钥不放入交接包。

## 4. 两个人具体怎么分

建议将较熟悉文档理解、证据检索和模型判断的同事设为A，将较熟悉文件处理和集成的同事设为B。以下为角色建议，未指派真实人员。

| 工作 | A：审查负责人 | B：工具与集成负责人 |
| --- | --- | --- |
| 公共接口与目录约定 | 提出审查所需字段，共同确认 | 维护统一约定和公共入口 |
| 文件审查 | 主责：规则整理、五份材料索引、逐项判断、证据、审查表输出 | 协助公共文件工具和接入 |
| 立项书起草 | 交叉检查内容来源和未知信息处理 | 主责：7组需求解析、分章生成、模板填充及版式 |
| Excel拆分 | 独立查看核对结果和异常案例 | 主责：拆分、校验、打包 |
| 集成与环境 | 提交审查模块依赖和调用说明 | 主责：演示目录、依赖、任务记录、技能安装与入口验证 |
| 联调验收 | 检查B的两个模块能否重跑 | 检查A的模块能否重跑；两人一起彩排 |

审查工作量最大，因此A只主攻这一项。B虽然负责两项业务和集成，但拆表较确定，统一入口沿用WorkBuddy；新增前端和复核服务会挤占B的主线时间，排在核心验收之后。

建议开工顺序：

1. 两人共同用半天以内确定目录、字段、输出命名和模型运行方式，并在演示电脑跑一次“读文件→执行脚本→产生可打开文件”。
2. A整理审查规则与证据索引；B先用拆表打通统一入口和任务记录。
3. A完成审查；B完成立项书。中途即交换可运行版本，不等全部做完才合并。
4. B合并到演示版本；两人互跑对方模块，修正环境、路径和数据格式问题。
5. 按拆表、审查、起草的顺序完成客户演示彩排。

## 5. 开发成果怎么交接

每个业务模块都按同一清单交付：

| 交接物 | 内容 | 接收方据此能做什么 |
| --- | --- | --- |
| 模块源码 | 解析、处理、生成、校验代码 | 在另一台电脑重复执行 |
| 任务说明 | 触发条件、步骤、参数、缺失信息处理、失败恢复 | 让WorkBuddy正确调用 |
| 提示词与规则 | 审查口径、起草约束、规则编号及版本 | 重现模型处理条件 |
| 模板 | 审查Excel、转换并检查过的Word副本 | 生成相同格式的文件 |
| 环境配置说明 | 依赖版本、转换工具、相对路径、配置样例 | 安装并配置运行环境 |
| 样例与验收记录 | 输入标识、预期结果、已知问题、运行记录 | 判断接入后是否退化 |

输出文件是业务成果；源码、模板、规则和说明构成可重复运行的能力。两者同时交付。

建议用同一个Git仓库管理代码和文本配置，每人开发独立模块，B维护公共目录并合并。缺少代码托管条件时，可先用带版本号的压缩包交换，每次附变更清单，由B维护唯一演示版本。客户原始素材单独保存，使用文件校验值识别版本。

目录示意（拟建结构，当前未创建这些模块）：

```text
中交Demo/
  README.md                 统一运行说明
  contracts/                输入输出字段约定
  modules/
    review/                 A负责
    proposal/               B负责
    split_excel/            B负责
  common/                   文件定位、任务记录和通用校验
  prompts/                  模型处理约束
  templates/                受控模板副本
  skills/                   三个场景的WorkBuddy调用说明
  config/                   不含密钥的配置样例
  acceptance/               场景与验收标准
  runs/<task_id>/            当次输入、中间数据、输出及日志
```

公共目录变更先通知对方，不能擅自改字段。路径以配置根目录或任务目录解析，不写死某位同事电脑的盘符。演示安装包需有版本号并记录依赖，避免“昨天在我电脑能用”成为交接依据。

## 6. 先约定输入输出，再写内部逻辑

以下为建议契约，不是现有系统已经提供的API。即使先用本地脚本，也使用相同字段；以后可由MCP或HTTP包装。

**三个业务统一输入：**

| 字段 | 作用 |
| --- | --- |
| schema_version | 识别输入输出格式版本 |
| task_id | 每次运行唯一编号 |
| business_type | review / proposal / split_excel |
| project_id | 可选业务项目编号；独立题目不强制共用 |
| input_files | 文件角色、任务内路径、文件校验值 |
| template_version | 本次使用的模板版本 |
| options | 审查阶段、所选需求、拆表字段等业务参数 |
| output_dir | 本次任务专属输出目录 |

**三个业务统一返回：**

| 字段 | 作用 |
| --- | --- |
| schema_version、task_id、business_type | 对应请求与契约版本 |
| status | running / needs_input / completed / failed |
| summary | 给业务人员看的简短说明 |
| artifacts | 文件名称、类型、任务内相对路径 |
| checks | 校验项、通过与否、差异说明 |
| warnings | 缺失资料、需人工确认事项 |
| result_path | 指向业务结构化明细 |

状态completed只表示本次处理完成。审查结果另设人工复核状态，例如pending / reviewed；生成了文件不代表专业审核通过，拆表存在差异也不能显示“全部一致”。

各模块在公共结构之外保留自己的明细：

- 审查：原表行标识、规则编号、判断、文档版本、章节/段落/表格位置、原文、整改建议。43行须均有处理状态；规则拆出的子要点另有覆盖关系。
- 起草：需求组编号、章节编号、正文、信息来源、待补充字段。保留需求到章节映射，方便复核是否遗漏。
- 拆表：单位名称、输入/输出/参考行数、字段差异、重复行次数、金额核对和异常行。

本地模式先让WorkBuddy返回本机实际可访问的文件位置；可下载性需在演示环境验证。若改成远程服务，服务端路径不能直接当作用户下载链接，必须提供文件ID和经过权限校验的下载方式。

## 7. 做出的业务成果怎么进系统

**当前Demo：** 每次运行先建立任务目录，再保存处理结果、业务文件、校验结果和版本信息。WorkBuddy展示当次任务摘要和文件入口。不同任务保留独立目录，同名报告不会覆盖上一轮结果。材料不足或校验失败要保留原因和可恢复的中间数据。

三个场景的对应关系如下：

| 场景 | 给人使用的文件 | 给后续系统使用的数据 |
| --- | --- | --- |
| 审查 | 原格式技术审查意见单.xlsx | 条款、证据、AI初审结果、建议、复核状态 |
| 起草 | 项目立项建议书_AI初稿.docx | 章节内容、需求映射、缺失事项、版本 |
| 拆表 | 50份Excel、ZIP、核对报告 | 单位与文件清单、校验状态、差异明细 |

**以后对接客户业务系统：** 复用上述数据，增加适配层完成“项目编号匹配→文件上传→结构化结果提交→待办创建→状态回读”。具体接口名称和字段以客户接口文档为准，现在不假设已经具备接口。

接收系统至少需要明确：项目/任务标识、文件上传下载方式、结果提交格式、复核结果查询方式，以及调用凭据。提交时携带任务ID和版本，避免重试造成重复待办。

MCP只负责把工具调用能力暴露给WorkBuddy；文件存储、复核状态和多人同时修改仍需业务服务实现。若做加分演示，使用明确标注的“模拟复核系统”：展示真实保存的报告、人工改动和状态。客户接口未验证前，不展示“已提交中交系统”。

## 8. 集成验收与时间安排

**最直接的成果交接验收：** A交付后，由B在统一演示环境从原输入重跑审查；B交付后，由A重跑立项书和拆表。结果必须由本次执行产生，并能用Office正常打开。

| 验收层次 | 必须检查的内容 |
| --- | --- |
| 环境 | WorkBuddy可读取素材、执行工具、保存和取回文件；模型可正常使用 |
| 接口 | 三模块读取同一契约，返回任务ID、状态、文件和校验信息 |
| 审查业务 | 43行全覆盖，适用子要点有记录，结论有规则和证据，原Excel版式保留；缺证不得误判通过 |
| 起草业务 | 7组需求有落点，九个主章节及附表保留，预算/日期/人员等未知信息标记待补充 |
| 拆表业务 | 当前样本50份、18,884行、20列；字段及重复次数全量核对，保留数据类型、原始精度及单位内顺序 |
| 异常 | 缺文件、缺分组字段、空单位、模型中断、输出校验失败均有明确状态，不静默丢数 |
| 可复现 | 从另一同事或演示电脑的新目录运行成功，不依赖私人盘符和聊天历史 |

建议以5个工作日作为条件满足后的内部准备估算，不是固定承诺：

| 时间 | A | B | 当天检查点 |
| --- | --- | --- | --- |
| 第1天 | 共同定契约；规则整理与材料解析 | 环境检查、公共任务结构、拆表 | WorkBuddy到真实文件的链路可运行 |
| 第2天 | 全量规则映射、证据索引、审查判断 | 需求解析、旧Word转换、章节映射 | 双方交换第一版结构化结果 |
| 第3天 | 审查表生成、缺证与跨文档检查 | 起草、Word生成、覆盖和版式检查 | 三个模块均进入联调 |
| 第4天 | 业务校验、互跑B的模块 | 集成三入口、互跑A的模块 | 从统一版本完整执行三项任务 |
| 第5天 | 核验原文、复核输出、配合彩排 | 修复环境问题、打包演示版本、计时 | 保存当次验收记录；有余量做模拟复核 |

如果工具执行能力、模型额度或旧Word格式转换不能满足要求，先记录受阻环节并调整计划，不能通过减少审查行或遗漏其他题目来宣称完成。

## 9. 依据及待验证事项

- 原始需求：测试场景素材中三份《场景说明.txt》。
- 素材数量、模板结构、验收细化：同目录《中交Demo方案.md》，采用另一任务已整理的结果。
- [WorkBuddy技能说明](https://www.workbuddy.cn/docs/workbuddy/From-Beginner-to-Expert-Guide/Function-Description/Skills-Market)：技能可封装可执行脚本及流程，并支持导入本地技能包。
- [WorkBuddy连接器文档](https://open.workbuddy.cn/docs/connector)：说明MCP及CLI接入路径、工具参数和结果约定。正式采用连接器时需按实际版本验证兼容性。

仍待现场核实：两人的工具与版本、最终演示账号和模型、工具执行与文件取回能力、是否具备独立模型API、模型处理时长与额度、客户验收口径。上述待验证项已有对应首日检查安排，不阻碍按此方案开始分工。

建议立即把A/B负责人、统一目录、输入输出契约和首个联调时间定下来，以“对方能重跑”为每项开发成果的交接标准。
