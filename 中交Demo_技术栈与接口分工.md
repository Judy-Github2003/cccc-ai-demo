# 中交 Demo：统一技术栈与接口分工

本文件依据用户确认的开发工具，调整《中交Demo_两人分工与成果对接方案.md》中“主要使用本地技能和脚本”的实现路径。本次更新为设计约定，尚未开发系统或执行接口测试。

## 1. 统一开发约定

| 层次 | 已确认工具 | 职责 |
| --- | --- | --- |
| 后端 | Java＋Spring | 文件上传、任务执行、模型调用编排、业务处理、成果生成、复核和下载接口 |
| 前端 | Next.js＋React | 三个业务入口、文件上传、执行状态、结果查看、复核编辑和下载 |
| 数据库 | MongoDB | 项目与任务记录、文件元数据、结构化结果、检查记录、人工复核状态 |
| 接口调试与测试 | Apifox | 接口定义、示例、Mock、联调和接口验收用例 |
| 版本管理与环境配置对齐 | Git | 同步代码、依赖版本、Docker配置、配置样例和运行说明，按同一提交版本联调 |
| 部署 | Docker | 封装前后端及数据库运行环境，保持开发和演示环境一致 |

Docker在此承担容器化部署职责。Java、Spring、Node.js、Next.js和MongoDB的具体版本在初始化项目时统一锁定；不让两位同事各自选择版本。Spring具体使用Spring Boot等形式待初始化时确定。

Next.js作为前端工程，业务判断和持久化由Java后端负责。前端不直接连接MongoDB，也不直接持有模型密钥。需要前端服务代理时只处理访问转发，不重复实现三类业务逻辑。

## 2. 一个系统中的调用关系

```text
浏览器 → Next.js / React业务页面 ─┐
                                 ├→ Java / Spring统一业务接口
WorkBuddy → 可选MCP适配层 ───────┘           ↓
                                 审查 / 起草 / 拆表模块
                                      ↓           ↓
                              MongoDB任务与结果   文件持久化目录
                                      ↑
                              审查与起草调用模型服务
```

浏览器承担完整可操作的业务界面。保留已确认的WorkBuddy接入方向，通过适配层调用同一后端能力，不再维护另一套审查、起草和拆表逻辑。

若本次验收仍要求在WorkBuddy完成三道题，WorkBuddy调用、输入文件传递、状态查询和文件取回均纳入交付；仅做成网页不能代替该项要求。复用现有连接能力或增加MCP适配层，具体实现以实际WorkBuddy版本验证结果为准。

文件上传到后端后用file_id引用。浏览器、WorkBuddy和容器之间不传递个人电脑的绝对文件路径。Docker使用持久化数据卷保存文件和MongoDB数据。

## 3. 两人按主责分工

| 人员 | 主责 | 必须交接的内容 |
| --- | --- | --- |
| A：后端与AI业务负责人 | Spring工程、MongoDB、上传下载、任务执行、审查模块、模型适配、其余两模块的后端实现、WorkBuddy适配 | 可运行后端、接口说明、结构化结果、业务文件、后端部署配置 |
| B：前端与联调负责人 | Next.js工程、三业务页面、任务与结果展示、人工编辑、Apifox接口文档与用例、前端部署配置；承担起草需求映射和拆表验收素材整理 | 可运行前端、公共数据类型、接口测试集合、样例、演示流程 |
| 两人共同 | 接口契约、规则与模板口径、模型联调、Docker联合部署、跨模块验收 | 一个可从输入重跑到输出的集成版本 |

这是按技术栈调整后的分工，不继续沿用“A只做审查，B包揽其他业务和全部集成”的旧分配。三个业务均需要前后端配合。

为减轻A的工作量，B在完成公共页面后，可按统一Java规范认领拆表模块；该调整应明确代码目录和接口负责人。A仍统一维护公共接口与后端集成，避免两人重复写上传、下载和模型配置。

界面采用同一页面中的三个业务标签，复用上传区、进度区和结果区。独立增加一个任务列表即可；复杂审批、多级权限和管理门户不作为本轮默认范围。

## 4. 先在Apifox对齐接口

建议统一前缀为 /api/v1。以下为拟定接口，不是已经上线的地址。

| 接口 | 用途 | 关键内容 |
| --- | --- | --- |
| POST /files | 上传输入文件或模板 | 返回file_id、原名、大小、校验值、文件角色 |
| POST /tasks | 提交审查、起草或拆表任务 | 接收business_type、input_file_ids、template_id、options；返回task_id和初始状态 |
| GET /tasks | 查询任务列表 | 支持业务类型与状态筛选、分页 |
| GET /tasks/{task_id} | 查询运行状态 | 返回状态、处理阶段、已完成数量、警告和错误 |
| GET /tasks/{task_id}/result | 查询结构化结果 | 返回各业务明细、核对信息、成果文件ID及结果版本 |
| GET /files/{file_id}/download | 下载输入或成果 | 后端定位文件；有访问控制时在此校验 |
| PATCH /tasks/{task_id}/review | 保存人工复核结果 | 审查项ID、修改意见、结果版本；AI原始结论单独保留 |
| POST /tasks/{task_id}/exports | 按指定结果版本生成文件 | 复核或修改后生成新的成果文件，返回导出任务编号 |

审查和起草属于长任务。提交接口保存任务后尽快返回，不让前端一直等待生成完毕。Demo先采用状态轮询；展示真实阶段或计数，无法计算准确进度时不显示虚构百分比。

导出接口同样按任务方式处理，使用任务查询接口查看状态和文件ID。修改结果后不能继续把旧Excel当作最新复核稿下载。

接口初版必须对齐：字段类型、必填项、枚举、错误格式、日期格式、文件大小限制、分页方式及接口版本。Apifox作为接口文档与联调入口，由B维护；A实现前共同确认。接口变更同步更新前端类型和测试用例。

样例和Mock阶段即可推进前端页面，但必须标记Mock状态，最终验收切换真实后端和当次生成文件。

## 5. 统一数据与任务状态

建议任务状态：queued、running、needs_input、completed、failed。人工复核状态单列pending、reviewed，不能用“任务完成”代表“审核通过”。

| MongoDB集合 | 建议保存内容 |
| --- | --- |
| tasks | 任务ID、业务类型、输入文件ID、配置、状态、当前阶段、创建更新时间、错误摘要 |
| files | 文件ID、原名、内部存储位置、校验值、大小、所属任务、文件角色、模板或成果版本 |
| review_items | 任务ID、原表行标识、规则与子要点、证据引用、AI结论、人工意见、版本 |
| proposal_sections | 任务ID、章节编号、正文、需求组映射、来源、待补充项、版本 |
| split_results | 任务ID、单位、成果文件ID、输入输出行数、校验状态和差异摘要 |

Word、Excel和ZIP先保存在Docker持久化文件卷，MongoDB保存元数据。长文档全文、全部经营明细及超长日志不塞进单条任务记录。需要集中对象存储时，通过文件服务替换存储实现。

三项原业务验收保持不变：

- 审查：覆盖现有方案记录的43个原表要求行及适用子要点，保留规则、出处、缺证状态和原Excel格式。
- 起草：覆盖7组需求，保留九个主章节及附表，未知预算、人员、日期等标记待补充。
- 拆表：当前样本18,884条明细、20列、50个单位；保留数据类型、精度、重复次数和单位内原行顺序，全量核验后打包。

上述数量继承另一任务已整理的素材核对结果，本次未重复检查全部原工作簿。

## 6. 个人GPT账号如何用于开发

两人给各自的ChatGPT提供同一版本的需求、技术栈、接口定义和开发约定，再分配各自模块。

公共开发说明应包含：

> 本项目后端Java＋Spring，前端Next.js＋React，数据库MongoDB，接口调试与测试使用Apifox，Git统一版本与环境配置，Docker部署。遵循项目接口契约；前端只通过Java接口获取业务数据，业务处理与文件生成由后端负责。只修改本次分配的模块，交付完整代码、配置样例、运行说明、验证记录和已知问题。

统一通过Git仓库合并实际成果。每次提交附上变更内容、影响接口、验证方式和未完成项。个人ChatGPT用于辅助开发，不作为系统可直接调用的模型服务。

两人的环境对齐约定：

- 同一Git仓库管理前后端代码、后端依赖及构建工具版本、前端依赖锁文件、Dockerfile、联合部署配置、配置样例、Apifox导出文件和运行说明。
- 各自在独立功能分支开发；合并前检查接口变化和相关用例。主分支保持可运行，演示版本使用Git标签标识。
- 联调时检出同一提交或标签，按仓库说明准备配置并构建、启动Docker环境；记录本次使用的版本。
- 真实密钥、本地环境配置、客户原始素材、运行产物和数据库数据目录通过.gitignore排除；仓库只提交不含秘密的配置样例。素材在指定位置单独共享，通过文件校验值对齐版本。
- Git同步的是环境定义与项目文件，不会自动复制本机软件、运行中的容器或数据库内容；运行环境由Docker配置重建，测试数据按统一准备步骤加载。

**模型运行接口仍待确认。** Java后端自主完成审查和起草，需要可供程序调用的模型服务；没有GPT团队版不能证明该接口存在或不存在。尚无接口时，可先用明确标注的Mock完成前后端联调和真实拆表，但不能宣称审查与起草已经端到端完成。若另选WorkBuddy内部模型编排，则需明确结构化内容提交协议与执行责任，不假定Java能调用其内部模型。

## 7. Docker与联调交付

建议Demo采用一份联合部署配置管理frontend、backend、mongodb三个服务；可用Docker Compose组织。文件存储和数据库配置持久化卷，运行密钥由环境配置注入。

暂不因使用Docker就增加Redis、消息队列或微服务。单实例后端可先采用受控后台执行，并把任务记录持久化；重启后识别中断任务，标记失败或允许人工重跑，不能让任务永久停在running。

最小集成交付包包括：前端源码、后端源码、联合部署文件、配置样例、Apifox接口与用例导出、规则及模板、演示素材说明、启动步骤、当次验收记录。不得包含真实模型密钥。

Apifox覆盖上传、任务提交、状态查询、结果、下载、复核和异常接口；前端交互及Word/Excel排版仍需浏览器和Office实际检查，接口返回成功不能代替业务验收。

## 8. 实施顺序和排期边界

1. 两人确认接口初版、目录、版本与模型服务条件；搭建前后端和数据库最小连接。
2. 优先做拆表：真实上传→创建任务→拆分及核验→页面显示→下载，贯通整条链路。
3. 沿用同一任务机制完成审查和起草，补齐证据展示与模板格式验证。
4. 接入WorkBuddy调用同一后端；执行真实输入输出和文件取回验证。
5. 核心三项稳定后增加人工复核和重新导出；完成Docker环境重建和彩排。

原方案五天估算基于较轻的WorkBuddy本地工具路径。现在新增前后端、MongoDB、接口与Docker集成，不能直接承诺同样工期。两人的熟练度、可复用代码、模型接口和实际截止日明确后，再估算人天；业务数量和验收范围保持不变。
